// Find the number is positive negative or zero using ternary operator
const number = 11
console.log("Number is ", (number === 0) ? "zero" : (number > 0) ? "positive" : "negative")
//let number = condition1 ? value1 : condition2 ? value2 : condition3 ? value3 : defaultValue;

